# ── 배포에 필요 없는 것만 제외한다 ────────────────────────────────────────
# 소스(src/)·SQL(db/)·문서(docs/)는 백업을 위해 함께 올린다.
# 사이트 구동에는 쓰이지 않지만, 이게 없으면 이 맥북 말고는 사이트를 고칠 수 없다.

# macOS
.DS_Store
._*

# 버린 파일 모음 — 로컬에만 둔다(옛 백업본·개발용·미사용 SQL·미배포 Edge Function)
_trash/

# 에디터
.vscode/
.idea/
